import { Route, Routes } from "react-router-dom";

export const Content = ({hoteles}) => {

    return(
        <div className="content-container">
            <Routes>

            </Routes>
        </div>
    )
}